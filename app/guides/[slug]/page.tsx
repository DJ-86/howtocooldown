import type { Metadata } from 'next';
import Link from 'next/link';
import { notFound } from 'next/navigation';
import { SiteHeader } from '@/components/SiteHeader';
import { articleBySlug, articles } from '@/lib/articles';
export const dynamicParams = false;
export function generateStaticParams() { return articles.map(({ slug }) => ({ slug })); }
type Props = { params: Promise<{ slug: string }> };
export async function generateMetadata({ params }: Props): Promise<Metadata> { const article = articleBySlug.get((await params).slug); if (!article) return {}; const path = `/guides/${article.slug}`; return { title: article.title, description: article.description, alternates: { canonical: path }, openGraph: { title: article.title, description: article.description, type: 'article', url: path, images: [] }, twitter: { card: 'summary', title: article.title, description: article.description, images: [] } }; }
export default async function ArticlePage({ params }: Props) { const article = articleBySlug.get((await params).slug); if (!article) notFound(); const related = [...article.relatedActions, ...article.relatedSituations, ...article.relatedScience, ...article.relatedSafety, ...article.relatedDecisions].map(slug => articleBySlug.get(slug)).filter(Boolean); const jsonLd = { '@context': 'https://schema.org', '@type': article.articleType === 'safety' ? 'MedicalWebPage' : 'Article', headline: article.title, description: article.description, datePublished: article.datePublished, dateModified: article.dateReviewed, mainEntityOfPage: `https://howtocooldown.com/guides/${article.slug}`, author: { '@type': 'Organization', name: 'How To Cool Down' } }; return <main className="min-h-screen bg-[#f4fbf9] text-[#173c3a]"><SiteHeader /><script type="application/ld+json" dangerouslySetInnerHTML={{ __html: JSON.stringify(jsonLd).replace(/</g, '\\u003c') }} /><article className="mx-auto max-w-3xl px-5 pb-24 pt-10 sm:px-8 sm:pt-16"><nav className="text-sm font-semibold text-[#68918a]" aria-label="Breadcrumb"><Link href="/">Home</Link> <span aria-hidden> / </span> <Link href="/guides">Guides</Link></nav><p className="mt-10 text-sm font-bold uppercase tracking-[.15em] text-[#ef6b4a]">{article.articleType === 'science' ? 'Why it works' : article.articleType === 'safety' ? 'Heat safety' : 'Action first'}</p><h1 className="mt-3 text-5xl font-extrabold leading-[1.02] tracking-[-.055em] sm:text-6xl">{article.title}</h1><section className="mt-8 rounded-2xl border-l-4 border-[#267969] bg-white p-6"><h2 className="text-sm font-bold uppercase tracking-[.13em] text-[#267969]">Short answer</h2><p className="mt-3 text-xl font-semibold leading-8">{article.shortAnswer}</p></section>{article.showCoolingTool && <aside className="mt-6 flex flex-col gap-4 rounded-2xl bg-[#173c3a] p-6 text-white sm:flex-row sm:items-center sm:justify-between"><div><p className="font-bold">Conditions change the answer.</p><p className="mt-1 text-sm text-[#cfe2de]">Compare indoor and outdoor temperatures with the cooling tool.</p></div><Link href="/" className="shrink-0 rounded-xl bg-[#ef6b4a] px-4 py-3 text-center font-bold">Check my room →</Link></aside>}<div className="mt-12 space-y-12">{article.sections.map(section => <section key={section.heading}><h2 className="text-3xl font-extrabold tracking-[-.04em]">{section.heading}</h2>{section.paragraphs?.map(p => <p key={p} className="mt-4 text-lg leading-8 text-[#415f5c]">{p}</p>)}{section.steps && <ul className="mt-5 space-y-3">{section.steps.map(step => <li key={step} className="flex gap-3 rounded-xl bg-white p-4 leading-7"><span className="font-bold text-[#ef6b4a]">✓</span><span>{step}</span></li>)}</ul>}{section.callout && <div className={`mt-5 rounded-2xl p-5 ${section.callout.tone === 'emergency' ? 'bg-[#a93d2d] text-white' : 'bg-[#fff1ed]'}`}><h3 className="font-bold">{section.callout.title}</h3><p className="mt-2 leading-7">{section.callout.text}</p></div>}</section>)}</div>{related.length > 0 && <section className="mt-14 border-t border-[#cfe2de] pt-10"><h2 className="text-2xl font-extrabold">Related guides</h2><div className="mt-4 grid gap-3 sm:grid-cols-2">{related.slice(0, 4).map(item => item && <Link key={item.slug} href={`/guides/${item.slug}`} className="rounded-xl bg-white p-4 font-bold text-[#267969]">{item.title} →</Link>)}</div></section>}<section className="mt-14 border-t border-[#cfe2de] pt-10"><div className="flex items-end justify-between gap-4"><h2 className="text-2xl font-extrabold">Sources & evidence</h2><p className="text-xs text-[#68918a]">Reviewed {article.dateReviewed}</p></div><ol className="mt-5 space-y-4">{article.sources.map(source => <li key={source.url} className="rounded-xl bg-white p-5"><a className="font-bold text-[#267969] underline underline-offset-4" href={source.url} target="_blank" rel="noreferrer">{source.organisation}: {source.title} ↗</a><p className="mt-2 text-sm leading-6 text-[#52716e]">Supports: {source.supports}</p><p className="mt-2 text-xs text-[#78928e]">{source.type} · Accessed {source.accessed}</p></li>)}</ol></section></article></main>; }
